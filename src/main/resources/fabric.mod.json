{
  "schemaVersion": 1,
  "id": "worldthreader",
  "version": "${version}",
  "name": "DimensionalThreading",
  "description": "An attempt to optimize the fabric server, by assigning each dimension their own thread.",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": [
      "_2no2name.worldthreader.WorldThreaderMod"
    ]
  },
  "mixins": [
    "worldthreader.mixins.json"
  ],
  "accessWidener": "worldthreader.accesswidener",
  "depends": {
    "fabricloader": ">=0.12.8",
    "minecraft": "1.20"
  }
}
