{
  "schemaVersion": 1,
  "id": "worldthreader",
  "version": "${version}",
  "name": "Worldthreader",
  "description": "Each dimension runs on a separate thread, speeding up each game tick.",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": [
      "no2.worldthreader.WorldThreaderMod"
    ]
  },
  "mixins": [
    "worldthreader.mixins.json"
  ],
  "accessWidener": "worldthreader.accesswidener",
  "depends": {
    "minecraft": "1.21.10"
  }
}
