{
  "schemaVersion": 1,
  "id": "worldthreader",
  "version": "${version}",
  "name": "Worldthreader",
  "description": "Unstable optimization mod that runs each dimension a separate thread.",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": [
      "no2.worldthreader.WorldThreaderMod"
    ]
  },
  "mixins": [
    "worldthreader.mixins.json"
  ],
  "accessWidener": "worldthreader.accesswidener",
  "depends": {
    "fabricloader": ">=0.16.5",
    "minecraft": ">=1.21"
  }
}
